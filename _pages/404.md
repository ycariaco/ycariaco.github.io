---
layout: page
permalink: /404.html
redirect: false
redirect_seconds: 8
---

<style>
  .post,
  .post-content,
  .container,
  .container-lg,
  .container-xl {
    max-width: none !important;
    width: 100% !important;
  }

  .post { padding-top: 0; }
</style>

<div style="width:100vw; height:100vh; margin-left: calc(50% - 50vw); display:flex; align-items:center; justify-content:center; overflow:hidden;">
  <img src="/assets/img/404.png"
       alt="404"
       style="height:100%; width:auto; max-width:100vw; object-fit:contain; display:block;" />
</div>

<p style="text-align:center; margin:0 0 8px;">
  You will be redirected to the main page in
  <span id="countdown">{{ page.redirect_seconds | default: 8 }}</span>
  seconds. If not redirected, please go back to the
  <a href="{{ site.baseurl | prepend: site.url }}">home page</a>.
</p>

<script>
(() => {
  let seconds = {{ page.redirect_seconds | default: 8 }};
  const el = document.getElementById('countdown');
  const timer = setInterval(() => {
    seconds -= 1;
    if (el) el.textContent = seconds;
    if (seconds <= 0) {
      clearInterval(timer);
      window.location.href = "{{ site.baseurl | prepend: site.url }}";
    }
  }, 1000);
})();
</script>
